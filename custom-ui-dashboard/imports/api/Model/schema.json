{
  "name": "Temperature",
  "description": "Temperature object schema",
  "bsonType": "object",
  "required": ["roomId","timestamp","temperature"],
  "properties": {
    "roomId": {
      "enum": [0,1,2,3,4,5,6],
      "bsonType": "int",
      "description": "must be a integer is required"
    },
    "timestamp": {
      "bsonType": "date",
      "description": "must be dateTime format <YYYY-mm-ddTHH:MM:ss> is required"
    },
    "temperature": {
      "bsonType": "double",
      "description": "must be a double is required"
    }
  }
}